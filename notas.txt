# Mandatory -> :
# Optional -> ?

Server
{
	port:
	host:
	server?
	error_pages:
	limit_client_body_size:
	routes:
		-list_of_ascepted_HTTP_methods:
		-define_HTTP_redirections:
		-define_seaarch_dir:
		-toogle_dir_listen:
		-default_file_to_answer_dir:
		-CGI:
}